name:

on:
  push:
    branches:
      -
  pull_request:
    branches:
      -

jobs:
  job_name:
    runs-on:

    steps:
      - name:
        uses:

      - name:
        run:

      - name:
        run: